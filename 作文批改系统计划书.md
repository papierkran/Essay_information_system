# 作文批改收集信息系统 — 计划书

## 一、项目概述

### 项目名称
作文批改收集信息系统

### 项目目标
构建一个面向培训机构的作文收集与批改管理平台，实现从收集到批改的完整流程线上化，**主要操作场景适配手机浏览器**。

### 目标用户
培训班班主任、助教、批改老师

---

## 二、角色与权限

| 角色 | 职责 | 权限范围 |
|------|------|---------|
| **管理员** | 系统管理 | 全部权限 |
| **收集者** | 负责班级的作文收集 | 仅限于自己有权限的班级，同班可有多名收集者 |
| **批改者** | 批改作文 | 可查看/批改被分配的任务 |

- 一人可多角色
- 一个收集者可负责多个班级
- **一个班级可有多名收集者**（如多个助教共同收集）
- 同班收集者共享收集任务（A 创建的任务 B 也能上传作文）
- 批改者无固定班级归属，按任务分配

## 三、组织架构

```
培训班
  ├── 写作一班（收集者：张三）
  ├── 写作二班（收集者：李四）
  └── 冲刺班（收集者：王五）
```

## 四、核心功能

### 4.1 用户与权限
- 注册/登录
- 角色分配与管理
- 一人多角色支持

### 4.2 收集管理
- 收集者直接上传作文，填写：班级、年级、第几次、作文标题、学生姓名、是否补交、备注
- 支持上传方式：文字粘贴 / docx / 拍照
- 查看本班已收集的作文列表
- 年份/月份/日期自动获取

### 4.3 批改管理
- 批改者查看待批作文列表（按班级/作文标题分组）
- 批改者在线查看作文原文
- 批改结果上传（**docx 文件**），自动记录批改时间
- 批改状态：待批 / 已批

### 4.4 手机适配
- 主要操作页面（上传作文、上传批改、状态查看）移动端适配
- 拍照/文件选择上传
- 卡片式列表布局

---

## 五、数据库设计

### 5.1 数据库选型
**SQLite**（轻量，无额外依赖，小规模足够）

### 5.2 表结构

#### users（用户表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| username | TEXT UNIQUE | 登录名 |
| password_hash | TEXT | 密码哈希 |
| nickname | TEXT | 昵称/姓名 |
| phone | TEXT | 手机号 |
| role | TEXT | 角色：admin / collector / reviewer，逗号分隔支持多角色 |
| org_id | INTEGER FK | 所属培训班 |
| is_active | INTEGER | 是否启用（1/0） |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

#### organizations（培训班表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| name | TEXT | 培训班名称 |
| desc | TEXT | 简介 |
| created_at | DATETIME | |
| updated_at | DATETIME | |

#### classes（班级表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| org_id | INTEGER FK | 所属培训班 |
| name | TEXT | 班级名称 |
| created_at | DATETIME | |
| updated_at | DATETIME | |

#### user_classes（用户-班级关联表，多对多）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| user_id | INTEGER FK | 用户 ID |
| class_id | INTEGER FK | 班级 ID |
| role_in_class | TEXT | 在该班的角色：collector / reviewer |

#### essays（作文表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| class_id | INTEGER FK | 所属班级 |
| grade | TEXT | 年级（初二/初三等） |
| essay_number | INTEGER | 第几次作文 |
| essay_title | TEXT | 作文标题 |
| student_name | TEXT | 学生姓名 |
| is_supplement | INTEGER | 是否补交（0/1） |
| remark | TEXT | 备注 |
| content_text | TEXT | 作文文字内容 |
| content_file | TEXT | 原文文件路径（主文件 docx/图片） |
| file_type | TEXT | 文件类型：text / docx / image |
| collected_by | INTEGER FK | 上传者（收集者） → users |
| status | TEXT | pending / corrected |
| reviewer_id | INTEGER FK | 批改者 → users |
| corrected_at | DATETIME | 批改完成时间 |
| created_at | DATETIME | 上传时间 |
| updated_at | DATETIME | 更新时间 |

#### 文件存储路径

```
uploads/
  └── {年份}/
      └── {月份}/
          └── {日期}/
              └── {班级名称}/
                  └── 第{第几次作文}次/
                      └── {收集者名称}/
                          ├── {作文标题}_{学生名}_第{第几次作文}次_{是否补交}_{备注}_{时间戳}.docx
                          └── {作文标题}_{学生名}_第{第几次作文}次_{是否补交}_{备注}_{时间戳}_img1.jpg
```

**批改结果**：批改者上传的文件放在同一目录下，文件名前缀加 `改_`：

```
改_{作文标题}_{学生名}_第{第几次作文}次_{是否补交}_{备注}_{时间戳}.docx
```

**判断是否已批改**：扫描目录下是否存在 `改_*.docx` 文件。

---

## 六、技术方案

| 层 | 选型 |
|---|------|
| **后端** | Python + FastAPI |
| **前端** | Vue 3 + Vite + 移动端 UI 框架 |
| **数据库** | SQLite |
| **文件存储** | 本地目录 |
| **部署** | PVE 上 Docker 化 |
| **ORM** | SQLAlchemy |

## 七、页面规划（移动端优先）

| 页面 | 角色 |
|------|------|
| 登录页 | 所有 |
| 工作台首页 | 所有 |
| 作文上传页 | 收集者 |
| 本班作文列表 | 收集者 |
| 待批作文列表 | 批改者 |
| 作文详情页 | 批改者 |
| 批改历史 | 批改者 |
| 用户管理 | 管理员 |
| 班级管理 | 管理员 |

---

*记录时间：2026-07-22*
