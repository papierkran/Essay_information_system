# 作文批改收集信息系统 — 计划书

## 一、项目概述

### 项目名称
作文批改收集信息系统

### 项目目标
构建一个面向培训机构的作文收集与批改管理平台，实现从收集到批改的完整流程线上化，**桌面端和手机端双端适配**。

### 目标用户
培训班管理员、助教（收集者）、批改老师（批改者）、游客

---

## 二、角色与权限

| 角色 | 标识 | 职责 | 权限范围 |
|------|------|------|---------|
| **管理员** | `admin` | 系统管理 | 全部权限：用户/班级/作文管理、系统设置、数据库备份、批量操作 |
| **收集者** | `collector` | 负责作文收集 | 上传作文（单个/批量）、查看作文列表、编辑/删除自己的作文 |
| **批改者** | `reviewer` | 批改作文 | 查看待批列表、认领批改、上传批改结果（文件+文字）、查看修改历史 |
| **游客** | `guest` | 只读查看 | 查看作文列表、待批列表、修改历史、作文详情（只读），不可下载/导出/编辑 |

- 一人可多角色（如 `"collector,reviewer"`，逗号分隔）
- `admin` 用户（username="admin"）为超级管理员，不可被其他管理员编辑/删除/改密
- 用户删除后，其作文 `collected_by` 自动转移给管理员

---

## 三、组织架构

```
培训班（Organization）
  ├── 写作一班（Class）  ← 收集者：张三、李四
  ├── 写作二班（Class）  ← 收集者：王五
  └── 冲刺班（Class）    ← 收集者：赵六
```

- 一个培训班下有多个班级
- 一个班级可分配多个收集者（多对多关系）
- 收集者同班共享任务（A 创建的任务 B 也能上传）

---

## 四、核心功能

### 4.1 用户与权限
- 登录（支持多账号共存，登录页快速切换）
- 角色分配（多角色复选框）
- 密码修改（管理员改他人 / 用户改自己）

### 4.2 收集任务管理
- 管理员创建收集任务（作文次数、年级、主题、课程名、提交方式、截止日期）
- 任务可激活/停用
- 上传时自动填充当前活跃任务信息

### 4.3 作文上传
- **单个上传**：填写年级、第几次、标题、学生姓名、补交、提交方式、备注，上传文件（docx/图片）或粘贴文字
- **批量上传**：按文件夹结构上传（子目录名=学生姓名），支持图片和 docx
- **批量上传批改结果**：上传批改后的 docx 文件，自动解析"修改前/修改后"文字
- 支持 `essay_id` 参数更新已有作文（重新上传）

### 4.4 批改管理
- 批改者查看待批作文列表
- 认领作文（claim）
- 上传批改结果：文件上传 + 文字批改（`corrected_text` 存入数据库）
- 批改状态自动同步（有 `改_` 前缀文件 → 自动标记已批改）

### 4.5 作文详情页
- 桌面端：左右分栏（修改前 | 修改后）
- 修改前：显示文字或图片，支持查看原文图片、下载原文、重新上传
- 修改后：显示批改文字，支持重新上传
- 字数统计开关（默认显示）
- 全屏模式（单屏/双屏）
- 段落渲染：按换行分段，首行缩进 2em，前两段居中加粗（标题+姓名行）

### 4.6 下载与导出
- **下载原文**：有文字内容时从 DB 生成 docx，纯图片时打包 zip
- **下载批改结果**：下载 `改_` 前缀文件
- **导出修改前后 docx**：从 DB 读取 content_text + corrected_text，生成规范格式 docx
  - 格式：宋体、小四（12pt）、行距最小值 12 磅、首行缩进 0.74cm
  - 结构：修改前 + 分页符 + 修改后
  - 文件名：`改_{标题}——{学生}第{N}次{线上/线下}{补交}.docx`
- **批量导出 docx**：选中多篇作文打包为 zip
- **导出 CSV**：当前筛选结果导出

### 4.7 作文列表
- 多条件筛选（姓名、标题、年级、次数、状态、提交方式、收集者、备注）
- 多列排序（收集时间、批改时间、学生、年级、次数）
- 分页 + 每页条数选择 + 页码跳转
- 批量操作：删除、导出 docx、更换收集者、更换任务
- 游客只显示「仅查看」

### 4.8 工作台（Dashboard）
- 快捷入口（上传、作文列表）
- 活跃收集任务卡片（含实时统计：总数/待批/已批/完成率）
- 最近上传列表

### 4.9 系统管理
- 用户管理（CRUD、角色分配、改密）
- 班级管理（CRUD、ClassIn CSV 导入）
- 系统设置（上传存储目录、数据库连接、API 地址）
- 数据库导出/导入（通过 SSH + pg_dump）

### 4.10 手机端适配
- Vant 4 组件库
- 卡片式列表布局
- 操作面板（Action Sheet / Dialog）
- 768px 响应式断点

---

## 五、数据库设计

### 5.1 数据库选型
**PostgreSQL**（生产环境） / SQLite（开发环境可选）

### 5.2 表结构

#### organizations（培训班表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| name | VARCHAR(100) | 培训班名称 |
| desc | TEXT | 简介 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

#### users（用户表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| username | VARCHAR(50) UNIQUE | 登录名 |
| password_hash | VARCHAR(200) | SHA-256 哈希（盐：essay_salt_） |
| nickname | VARCHAR(50) | 昵称/姓名 |
| phone | VARCHAR(20) | 手机号 |
| role | VARCHAR(50) | 角色：admin/collector/reviewer/guest，逗号分隔支持多角色 |
| org_id | INTEGER FK | 所属培训班 |
| is_active | BOOLEAN | 是否启用 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

#### classes（班级表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| org_id | INTEGER FK | 所属培训班 |
| name | VARCHAR(100) | 班级名称 |
| created_at | DATETIME | |
| updated_at | DATETIME | |

#### user_classes（用户-班级关联表，多对多）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | |
| user_id | INTEGER FK | 用户 ID |
| class_id | INTEGER FK | 班级 ID |
| role_in_class | VARCHAR(20) | 在该班的角色（默认 collector） |

#### essay_tasks（收集任务表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| name | VARCHAR(100) | 任务名称 |
| grade | VARCHAR(20) | 年级 |
| essay_number | INTEGER | 第几次作文 |
| essay_topic | VARCHAR(200) | 作文主题 |
| course_name | VARCHAR(100) | 课程名 |
| teaching_mode | VARCHAR(10) | 线下/线上 |
| deadline | DATETIME | 截止日期 |
| is_active | BOOLEAN | 是否激活 |
| created_at | DATETIME | |
| updated_at | DATETIME | |

#### essays（作文表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| class_id | INTEGER FK | 所属班级 |
| task_id | INTEGER FK | 关联收集任务 |
| grade | VARCHAR(20) | 年级 |
| essay_number | INTEGER | 第几次作文 |
| essay_title | VARCHAR(200) | 作文标题 |
| student_name | VARCHAR(50) | 学生姓名 |
| is_supplement | BOOLEAN | 是否补交 |
| teaching_mode | VARCHAR(10) | 线下/线上 |
| remark | TEXT | 备注 |
| content_text | TEXT | 作文文字内容 |
| content_file | VARCHAR(500) | 原文文件路径（相对路径） |
| file_type | VARCHAR(10) | text / docx / image |
| collected_by | INTEGER FK | 上传者 |
| status | VARCHAR(20) | pending / corrected |
| file_saved | BOOLEAN | 文件是否存在于磁盘 |
| corrected_text | TEXT | 批改文字内容 |
| reviewer_id | INTEGER FK | 批改者 |
| corrected_at | DATETIME | 批改完成时间 |
| created_at | DATETIME | 上传时间 |
| updated_at | DATETIME | 更新时间 |

#### operation_logs（操作日志表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| essay_id | INTEGER FK | 关联作文 |
| user_id | INTEGER FK | 操作者 |
| action | VARCHAR(20) | 操作类型：上传/认领/修改/编辑/删除/恢复 |
| detail | VARCHAR(500) | 操作详情 |
| created_at | DATETIME | 操作时间 |

---

## 六、文件存储结构

```
{upload_dir}/
  {年份}/
    {月}月/
      {日}/
        {年级}{线上/线下}第{N}次/
          {收集者姓名}/
            {学生姓名}/
              1.jpg                    ← 原文图片（按序重命名）
              2.jpg
              {标题}_{姓名}_{次数}_{补交}_{备注}_{时间戳}.docx  ← 原文 docx
              改_{原文件名}.docx       ← 批改结果文件
```

- 批改结果文件名前缀 `改_`，用于自动判断是否已批改
- 启动时自动同步 `file_saved` 状态（标记文件是否存在于磁盘，不删除记录）
- 修改作文元数据（年级/次数/姓名/提交方式）时，文件自动移动到新目录
- 修改上传存储目录时，数据库中有记录的文件自动迁移到新目录

---

## 七、技术方案

| 层 | 选型 |
|---|------|
| **后端** | Python 3.10+ + FastAPI + SQLAlchemy + PostgreSQL |
| **前端** | Vue 3 (Composition API) + Vite 8 + Vant 4 + Axios |
| **认证** | JWT (python-jose + passlib)，30 天有效期 |
| **文件生成** | python-docx（文字转 docx，宋体小四规范格式） |
| **前端图表** | ECharts + vue-echarts |
| **前端打包** | JSZip（批量导出） |
| **部署** | Nginx 反向代理 + PostgreSQL |

## 八、页面规划

| 路由 | 页面 | 可用角色 |
|------|------|---------|
| `/login` | 登录页（多账号切换、服务器地址配置） | 所有 |
| `/dashboard` | 工作台首页（快捷入口、活跃任务、最近上传） | 所有登录用户 |
| `/essay/upload` | 单个上传作文 | 收集者、管理员、游客（只看） |
| `/essay/batch-upload` | 批量上传（作文 + 批改结果） | 收集者、管理员、游客（只看） |
| `/essay/list` | 作文列表（筛选/排序/分页/批量操作） | 收集者、管理员、游客（只看） |
| `/review/pending` | 待批作文列表 | 批改者、管理员、游客（只看） |
| `/review/detail/:id` | 作文详情（修改前/后、编辑、上传批改） | 批改者、收集者、管理员、游客（只看） |
| `/review/operations` | 操作日志历史 | 批改者、管理员、游客（只看） |
| `/admin/users` | 用户管理 | 管理员 |
| `/admin/classes` | 班级管理 + 收集任务管理 | 管理员 |
| `/admin/settings` | 系统设置（存储目录、数据库、备份） | 管理员 |

---

## 九、导出格式规范

导出的 docx 文件统一格式：

| 属性 | 值 |
|------|-----|
| 字体 | 宋体（含东亚字体 eastAsia） |
| 字号 | 小四 = 12pt |
| 行距 | 最小值 12 磅 |
| 首行缩进 | 0.74cm（正文段落） |
| 段前/段后 | 0 |
| 标题行（前 2 段） | 居中 + 加粗，无缩进 |
| 修改前/修改后 标题 | 宋体 12pt，不加粗不居中 |

---

*更新时间：2026-07-26*
