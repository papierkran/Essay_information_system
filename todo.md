# 作文收集管理系统 · 需求调研与改进清单

> 状态说明：本文件为需求调研与改进记录（2026-08-06）。
> **当前阶段：已实施 🔴 安全修复（S1-S8），其余项仍待规划/确认。**

---

## 一、项目背景（用户访谈确认）

- 使用场景：**单家培训机构内部使用**（非多校区/多机构，无需数据隔离）
- 数据库：生产环境使用 **PostgreSQL**
- 用户现状：对系统整体**满意度低**，认为现有功能「都不够好」

## 二、用户明确表达的核心痛点（按用户原话归纳）

1. **数据管理混乱**
   - 学生/班级/任务数据重复、不一致，缺乏统一维护入口
   - 同一学生作文分散在多个任务，难以按学生维度汇总成长轨迹
   - 无数据清洗、合并、迁移手段
2. **权限与数据安全**
   - 对权限范围、数据泄露风险不放心（详见代码审查清单）
3. **系统不稳定 / 有 Bug**
   - 大数据量时卡顿/崩溃
   - 手机端/浏览器兼容性差
   - 无容错与恢复机制
   - 错误提示不足
4. **缺核心报表/统计**
   - 需要批改工作量/进度报表
5. 批改流程（待批/认领/批改/归档）**用户目前认为尚可**，暂不作为重点

## 三、代码审查发现的问题清单

### 🔴 安全（已修复 2026-08-06）

| # | 问题 | 位置 | 修复内容 |
|---|------|------|------|
| S1 | ~~越权漏洞：公开注册接口可自选角色~~ | `backend/app/api/auth.py:12-31` | 已修复：注册接口忽略客户端传入 role，强制为 `collector`，无法自选 admin 提权 |
| S2 | ~~密码哈希不安全~~ | `backend/app/utils/auth.py:19-20` | 已修复：改用 bcrypt（`bcrypt.hashpw/gensalt`），登录兼容旧 SHA-256 哈希（检测到 `$2` 前缀走 bcrypt，否则回退 legacy 校验） |
| S3 | ~~JWT 默认密钥兜底~~ | `backend/app/utils/auth.py:12` | 已修复：`ESSAY_ENV=production` 且未设置 `ESSAY_JWT_SECRET` 时拒绝启动；开发环境不再使用原硬编码密钥 |
| S4 | ~~CORS 配置错误~~ | `backend/app/main.py:11-18` | 已修复：弃用 `allow_origins=["*"]`，改为 `ESSAY_CORS_ORIGINS` 白名单（逗号分隔），默认仅本机 dev 端口 |
| S5 | ~~公开接口泄露任务信息~~ | `backend/app/api/essays.py:38-48` | 已修复：`/api/essays/tasks/active` 增加 `get_current_user` 鉴权 |
| S6 | ~~API 密钥明文存储~~ | `system_config` 表（OCR/LLM） | 已修复：新增 `utils/crypto_utils.py`（Fernet 加密），写库时对 `api_key/secret/token/password` 等敏感字段加密，读取时解密；密钥来自 `ESSAY_CRYPTO_SECRET`/`ESSAY_JWT_SECRET`，否则持久化随机密钥文件 `backend/.crypto.key`（已 gitignore） |
| S7 | ~~路径穿越风险~~ | `backend/app/utils/file_utils.py:68` | 已修复：新增 `safe_component()` 清洗 `../`、`/`、`\`，应用于 `get_essay_dir`、`generate_essay_filename`、上传/批量导入路径及 `/{essay_id}/file/{filename}`（`os.path.basename`） |
| S8 | ~~登录口令明文传输~~ | 部署层 | 已缓解：后端无法强制 HTTPS，已通过 `.env.example` 与 `main.py` 生产环境提示（`ESSAY_TLS=https`），需在 Nginx/Caddy 等反向代理层启用 HTTPS |

### 🟠 性能（大数据量卡顿/崩溃的根因）

| # | 问题 | 位置 | 说明 |
|---|------|------|------|
| P1 | **N+1 文件系统查询** | `essays.py:671` + `_essay_to_out` (2125-2171) | 列表每行都 `has_correction`/`count_corrections_in_dir` 逐个 stat 磁盘 |
| P2 | **重复全局统计** | `essays.py:673-675` | 每次列表请求重复 3 次全表 count（pending/confirming/corrected） |
| P3 | **作文图片存 DB** | `EssayImage.image_data = LargeBinary` (models.py:173) | 数据库体积膨胀、查询与备份变慢，应改存文件系统 |
| P4 | **字数筛选全表扫描** | `essays.py:627-634` | `char_length(content_text)` 无法走索引 |
| P5 | **操作日志 N+1** | `essays.py:1965-1966` | 每行日志单独查 user/essay |
| P6 | **启动全量文件同步** | `main.py:29-38` | 每次启动遍历全部作文做 stat，数据量大时启动慢 |
| P7 | **批量操作全量加载** | 多处 `db.query(Essay).filter(id.in_(...)).all()` | 大批量时内存风险，建议分批处理 |
| P8 | **后台任务无队列** | `import threading` | OCR/LLM 用裸线程，无队列/限流/进度持久化 |

### 🟡 稳定性 / 数据管理

| # | 问题 | 位置 | 说明 |
|---|------|------|------|
| D1 | **无版本化迁移** | `database.py:_migrate_existing_columns` | 手写 ALTER 补列，schema 演进风险高，建议引入 Alembic |
| D2 | **数据库密码明文** | `settings.json` / `database.py:41` | 连接串拼接未对密码特殊字符转义，且配置明文落盘 |
| D3 | **无学生实体表（数据混乱根源）** | models.py 无 Student 表 | 学生只是 Essay 上的字符串字段 → 无法名单维护、跨任务汇总、成长轨迹 |
| D4 | **唯一约束过严** | models.py:149-150 | (class+task+student+number+supp+title) 六元唯一，批量导入易冲突 |
| D5 | 无自动备份/恢复保障 | - | 仅系统设置手动备份，无定时自动备份策略 |
| D6 | 无统一错误处理与日志落盘 | - | 错误提示不足（用户反馈），缺审计日志文件 |

## 四、需求清单（用户提出，待实施）

> 以下均为「暂不改动、先记录」的诉求。

### R1. 安全加固 ✅ 已实施（2026-08-06，含 S1-S8）
- ✅ 关闭公开注册越权提权（注册固定 `collector` 角色）
- ✅ 密码哈希升级为 bcrypt（兼容旧哈希）
- ✅ 生产环境强制 JWT 密钥配置
- ✅ 修正 CORS；鉴权敏感接口（tasks/active）
- ✅ API 密钥加密存储（Fernet）
- ✅ 路径穿越防护（S7）
- ⚠️ S8 HTTPS 为部署层事项，已给出配置项与提示，需在反代层落地
- ⚠️ 存量管理员账号可继续使用（bcrypt 兼容旧 SHA-256）；建议重置一遍密码以统一为 bcrypt

### R2. 性能优化
- 消除 N+1（文件系统查询、日志列表）
- 作文图片由 DB 迁移到文件系统
- 为常用筛选加索引
- 批量操作分页处理

### R3. 稳定性/备份
- 引入 Alembic 版本化迁移
- 定时自动备份数据库与文件
- 统一错误处理与日志

### R4. 报表（用户已确认需要）
- **批改工作量/进度报表**：
  - 按班级/任务/时间维度统计批改完成率
  - 批改者工作量统计（批改篇数、耗时）
- 一键导出 Excel
- （待确认：学生成长/缺交统计是否纳入）

### R5. 学生名单管理（用户暂缓）
- 建立学生实体表、班级名单维护、按学生归集作文与成长轨迹
- **用户表态：暂不动，先记录**

### R6. 评分与评语体系（行业对标新增，已确认）
- 批改结果增加分数/等级/评语（可配评语模板），形成可量化批改结果
- 关联：后续可支撑工作量报表与成长统计

### R7. 逐句对照视图（行业对标新增，已确认）
- 一键按钮将修改前后内容生成**逐行逐句对比**（diff 高亮修改点）
- **用户明确：功能复杂度高，优先级往后排**

### R8. 日志与监控（行业对标新增，已确认）
- 结构化日志 + 错误追踪，快速定位故障
- 对应"错误提示不足"痛点，含前端错误提示统一优化

### R9. 自动备份（行业对标新增，已确认）
- 定时自动备份数据库与文件，保留 N 份策略

> 行业对标中以下方向用户已明确**暂不需要**：
> - 在线批注/圈画、错误归类统计、多版本管理（批改体验其他项）
> - 缺交名单/催交、任务分配/转派（教学管理）
> - 消息通知、批改快捷键、批量批改导入（效率/通知；注：批量批改导入**现有功能已支持**）
> - Docker 部署、自动化测试（工程其他项）

### R10. 课程实体化（已实施 2026-08-06）
- **设计决策**：课程复用 `classes` 表，不建独立课程表（用户确认"一课程多任务、线上线下同一课程"）
- **已实施改动**：
  - `essay_tasks`：移除 `course_name` 字符串字段，新增 `course_id`（FK→classes.id）；`course_name` 改为 ORM 计算属性
  - `essays`：新增 `course_id`（FK→classes.id，直接关联课程，上传时优先取传入值否则从任务继承，属冗余存储）
  - 同步更新：schemas（TaskOut/EssayOut 含 course_id/course_name）、essays.py 上传继承逻辑、admin.py 任务创建/更新
- **后续演进（2026-08-06 二期）**：`Class` 模型重构为 `Course`（表 `course`），FK 全部指向 `course.id`；`essays.class_id` 与 `course_id` 双列并存（过渡期，`class_id` 仍 NOT NULL）
- **遗留风险**：
  - `essays.course_id` 暂无索引
  - `essays.class_id` 与 `course_id` 双列语义未彻底分离（详见 sql.md 方案 A/B）
  - `user_classes` 半启用（后端接口在、前端无调用，数据可见性未生效）
  - 存量库 `classes` 表需迁移（RENAME 或新建拷贝）
- 对应 sql.md 已同步更新

### R11. 未改列表任务筛选搜索（已确认并支持）
- **确认**：未改列表（ReviewPending）的「任务」筛选需要支持搜索（与作文列表一致）
- **状态**：现有实现已支持——前端 `frontend/src/views/ReviewPending.vue` 任务下拉框输入即时过滤（`filterTaskSearch`/`filteredTaskOptions`），回车发送 `task_name` 参数；后端 `/api/essays/pending` 已支持 `task_name` 模糊过滤（`essays.py:740-741`），无需额外改动

## 五、用户给出的优先级

用户明确要求：**先记录需求，本轮不做改动**。

规划顺序建议（待用户确认）：
1. ✅ 🔴 安全修复（S1-S8）——**已完成 2026-08-06**
2. 🟠 性能优化（P1-P8）
3. 🟡 稳定性与备份（D1-D6，含 R9 自动备份、R8 日志）
4. R4 报表（批改工作量/进度）
5. R6 评分与评语体系
6. R7 逐句对照视图（**优先级靠后**，复杂度高）
7. R5 学生名单管理（暂缓）

## 六、待确认问题（Open Questions）

- [x] 公开注册接口：机构内部是否仍需要自助注册？还是改为仅管理员创建账号？（安全修复已限制注册角色为 collector，是否彻底关闭公开注册待定）
- [ ] 数据可见范围：目前收集者可看全部作文，是否需要收窄为「只看自己收集/自己班级」？
- [ ] 图片存储：是否同意将作文图片从数据库迁出到文件系统？
- [ ] 报表粒度：批改工作量报表需要哪些具体指标（完成率/篇数/字数/耗时/漏改率）？
- [ ] 学生成长/缺交统计报表是否要一并纳入本轮？
- [ ] 自动备份周期与保留份数如何设定？
- [ ] 手机端兼容性具体在哪些机型/浏览器出现问题？（需复现案例）
- [ ] 评分体系：分数制（百分制）还是等级制（优/良/中/差）？是否要评语模板库？
- [ ] 逐句对照：对比基于纯文本 diff 即可，还是需要解析 docx 段落结构？（影响复杂度与排期）

## 七、实施规范（后续开发时遵守）

- 未获用户确认前，不实施任何改动
- 每轮改动范围需与用户确认后再动手
- 改动用例：性能优化、安全修复、报表开发均需回归验证
- 代码不新增注释（遵循现有风格），遵循仓库既有规范
