# 作文收集管理系统 · 改进清单

> 状态说明：本文件为需求调研与改进记录。
> **最近更新（2026-08-15）：完成第二轮前端用户视角体检，新增 H1–H37 与 L1–L14（见「二、前端用户视角体检第二轮（2026-08-15）」）；🔴 高优先级 H1–H12 已实施完毕，H13–H36/L1–L14 尚未实施。**
> 上一轮（2026-08-14）：G1–G38（G21/G22 暂缓）、G39/G41/G42/G44–G51/G55–G57（G40/G43/G52/G53/G54 暂缓）已全部实施完毕并从清单删除；G40/G43 于 2026-08-15 复核仍存在。

---

## 一、用户视角体检新增（2026-08-14）

> 从"看得懂、用得准、用得顺、不丢数据"四个角度审查 `frontend/src` 全部页面与 `backend/app` 后整理。

### 🔴 高优先级（数据风险 / 权限漏洞 / 功能必然失败）

> ✅ G1–G10 已全部完成并从本表移除。

### 🟠 中优先级（准确性与一致性）

| # | 问题 | 位置 | 说明 |
|---|------|------|------|
| G21 | 数据库导出超时未捕获、硬编码 root | `admin.py:479-488` | 大库导出超时 500 裸错误；远程主机非 root 免密失败。应捕获超时 + SSH 用户可配置 |
| G22 | 下载文件名含 Windows 非法字符 | `essays.py:184-193,1743` | 标题含 `/ \ : * ? " < > \|` 时 Windows 保存/解压失败。应清洗文件名 |

### 🟡 低优先级（体验提升）

> ✅ G39、G41、G42、G44–G51、G55–G57 已实施并从本表移除；G40/G43/G52/G53/G54 按用户要求暂缓。

| # | 问题 | 位置 | 说明 |
|---|------|------|------|
| G40 | 「图片不超4MB」提示与实际不符 | `EssayUpload.vue:49` | 实际限制 8MB，统一口径（2026-08-15 复核仍存在，并入 H14 周边） |
| G43 | 操作历史无 essay_id 行点击无反应 | `ReviewHistory.vue:222-226` | 应禁用或 toast「无关联作文」（2026-08-15 复核仍存在） |
| G52 | 下载批改结果缺游客拦截 | `essays.py:1427-1450` | 与其它下载不一致，应补 guest 拦截 |
| G53 | CORS 开发环境任意来源+带凭证 | `main.py:21-27` | 内网部署时任意站可发凭证请求，应收紧 |
| G54 | settings.json 明文保存数据库密码 | `admin.py:363-364` | 应加密或仅存引用 |

---

## 二、前端用户视角体检第二轮（2026-08-15）

> 从"看得懂、用得准、用得顺、不丢数据"四个维度，逐页复核 `frontend/src/views` 全部页面（含桌面/手机双端）+ 全局基础设施（`main.js`/`App.vue`/`api`/`composables`/`TaskStatusBar`）。
> 结论：功能覆盖已相当完整（筛选持久化、列配置、拖拽勾选、离开保护、批量任务进度等都已具备），本轮问题集中在 **数据安全（覆盖/删除无确认、静默丢数据）、提示与实际规则不一致、批量操作闭环、移动端体验** 四类。编号 H=高/中、L=低。
> **实施进度：🔴 高优先级 H1–H12 已于 2026-08-15 全部实施完毕（条目保留备查）；H13–H36 与 L1–L14 尚未实施。**
> 已复核确认：G40（4MB/8MB 提示）与 G43（操作历史无 essay_id 点击无反应）仍存在，并入下表相关条目。

### 🔴 高优先级（数据风险 / 功能必然失败）

> ✅ H1–H12 已于 2026-08-15 全部实施完毕（前端 6 个文件 + 后端 2 个文件），条目保留备查。

| # | 问题 | 位置 | 说明 |
|---|------|------|------|
| H1 | .doc 前端放行、后端静默丢弃 → 空作文 | `EssayUpload.vue:48,337`、`EssayBatchUpload.vue:512`、`essays.py:547-581` | 两处上传都允许选 .doc 并显示"已加入/上传成功"，后端无 .doc 处理分支，作文以空内容落库；批量页提示「不支持 .doc」与代码矛盾。应在前端拦截并 toast「请另存为 .docx」 |
| H2 | 批量页「仍会按 409 兜底跳过」与实现不符 | `EssayBatchUpload.vue:88,799` | 仅「跳过已存在」开关开启才把 409 当跳过；关闭时重名学生记失败，「仅重试失败」重传同批再次 409 → 死循环。应一律按跳过处理或修正文案 |
| H3 | AI 一键修改/OCR/错别字修正无确认直接覆盖 | `ReviewDetail.vue:250-252,393,792-816,871-886` | 三处操作均无确认弹窗：一键修改直接覆盖已手工精修的修改后内容，OCR/修正覆盖原文，且无 diff 预览、无撤销 |
| H4 | OCR/重传成功后 loadEssay 吞掉未保存表单 | `ReviewDetail.vue:956-967`（配合 792-816/1266-1317） | 收集者改好姓名/标题未保存就点 OCR，结果覆盖原文的同时未保存修改被静默重置，isDirty 变 false 不再提醒 |
| H5 | 非批改角色看到「✅ 已修改 / 修改完成于 undefined」 | `ReviewDetail.vue:115,140-153` | 游客/收集者看 pending/confirming 作文时右侧卡片写死"已修改"，corrected_at 为空时显示 "undefined" |
| H6 | 「修改任务」弹窗默认选中「无任务」且无校验 | `EssayList.vue:273,310,827` | 打开弹窗即高亮"无任务"，直接确定会把全部选中作文 task_id 清空，无二次确认；与「修改收集者」的必选校验不一致 |
| H7 | 手机端批量任务无进度反馈 + 批量确认无确认弹窗 | `ReviewPending.vue:204-221,529-542` | 任务进度面板仅桌面渲染，手机启动批量任务后无任何反馈；batchConfirm 直接提交、可连点，是全页唯一无确认的批量操作 |
| H8 | AdminTasks 模板调用未定义的 applyFilter | `AdminTasks.vue:15,19,23,28` | 切换年级/第几次/提交方式/状态下拉必抛 `applyFilter is not a function`（computed 仍生效，但每次触发报错） |
| H9 | AdminSettings「检测连接」测的是已保存配置 | `AdminSettings.vue:311-351` | testServer 读 localStorage、testDb 不带表单参数：用户改完新配置点检测得到旧结果，被明确误导 |
| H10 | 批量页图片压缩串行执行且无进度 | `EssayBatchUpload.vue:533`（518-536 循环内） | 上百张大图逐张同步解码/编码，界面无 loading，用户以为卡死；对比"修改后"模式有解析进度条 |
| H11 | 批量上传完成无终态，可重复提交整批 | `EssayBatchUpload.vue:133-136,322-325` | 结果弹窗关闭后按钮仍可点；预检关闭时整批重传全部 409 失败，缺"已上传完成/选择下一批"状态 |
| H12 | 「同时删除本地文件」实为「彻底删除」 | `EssayList.vue:224-225,863,878` | 复选框文案只提"删除本地文件"，实际同时置 permanent=true 彻底删除不进回收站，无不可恢复警示 |

### 🟠 中优先级（准确性与一致性）

| # | 问题 | 位置 | 说明 |
|---|------|------|------|
| H13 | 上传页可提交"空作文" | `EssayUpload.vue:414-425` | 只填标题+姓名、不选文件也不粘文字即可提交成功；应校验「文件或文字至少一项」 |
| H14 | 标题自动识别静默回填、无提示 | `EssayUpload.vue:268-273,58` | 首段被自动当标题且无"已自动识别"标记；手动清空标题后再编辑正文会被悄悄回填；预览加粗前 2 段与"取第 1 段"规则不一致 |
| H15 | 上传页无刷新/离开保护与草稿 | `EssayUpload.vue` 全页 | 长文本+多图误刷新/误返回全部丢失；应草稿持久化 + beforeunload |
| H16 | 批量页文件夹解析仅支持两级 | `EssayBatchUpload.vue:525,496-504` | 按「年份/课程/第几次/学生」四级组织时 parts[1] 被当学生名，姓名全错；年级/次数在深层目录不会被解析。提示与宣称能力不符 |
| H17 | 批量页无法预览/编辑标题与学生名 | `EssayBatchUpload.vue:95-100,742-763` | 预览只有姓名+文件数，docx/txt 首行标题不展示、不能改名；上传后这批作文标题为空 |
| H18 | 修改后模式重复上传静默覆盖 | `EssayBatchUpload.vue:829-888`、`essays.py:679-697` | 同一学生重传直接 UPDATE 原记录（含补交标记/状态），无任何提示；与作文模式 409 语义相反 |
| H19 | 列表筛选持久化不按用户隔离 | `EssayList.vue:370-386,445-476` | FILTER_KEY/列配置用固定 key，共享电脑上 A 的筛选（含 collectedBy=自己）会被 B 原样恢复 |
| H20 | 跨页勾选范围不可见、批量删除确认仅总数 | `EssayList.vue:78,219-223,500,580` | 跨页选中只显示"已选 N 条"，全选框无 indeterminate；删除确认列不出学生/标题清单，误删无撤销 |
| H21 | 非管理员默认只看自己收集的作文 | `EssayList.vue:363-366,918-923` | 纯批改者进列表默认被限定收集者=自己 → 空列表 +「请调整筛选条件」误导文案 |
| H22 | 移动端筛选 14 项全展开 + 每页可调 1000 行 | `EssayList.vue:6-69,89-95,127` | 手机端筛选器全部堆叠在列表上方；每页 1000 行全量渲染（约 1.3 万单元格）无虚拟滚动 |
| H23 | 详情页确认/重改可自我确认、无 loading | `ReviewDetail.vue:133-138,1167-1216` | 确认/重改仅 reviewer/admin 可见且 reviewer 可自我确认（两步流转失去制衡）；两函数无防重复 |
| H24 | isDirty 漏掉批改输入区 | `ReviewDetail.vue:620-637` | correctionText/correctionNote/reuploadCorrectedText/selectedFile/手机端待传文件不在 dirty 检测内，大段批改文字误触返回/刷新即丢 |
| H25 | 取消编辑丢内容；corrected 后仍可重新上传 | `ReviewDetail.vue:189,255,249,277-296,412` | 点"✕ 取消"直接丢弃已输入内容无确认；已确认完成（corrected）后桌面仍可重新上传并提交，手机端整块消失，两端能力不一致 |
| H26 | 批量任务失败明细不可见 | `TaskStatusBar.vue`、`useTaskMonitor.js:49-67` | 后端已返回 errors（学生名+原因），前端只显示计数/消息，用户无法知道哪些失败、无法只重试失败项 |
| H27 | 工作台「最近上传」不可点击 | `Dashboard.vue:71-93` | 列表行无跳转入口，无法从最近上传直达详情 |
| H28 | AdminSettings 配置加载失败静默降级 | `AdminSettings.vue:353-432` | 加载失败时表单显示默认值，用户保存会整体覆盖线上真实配置；应提示并禁用保存 |
| H29 | AdminSettings 无未保存离开保护 | `AdminSettings.vue` 全页（约 30 输入框） | 填了一半的 API Key 切走/刷新全部丢失 |
| H30 | AdminTasks 状态判定与删除失败处理 | `AdminTasks.vue:404-409,496-503` | 未设开始时间+未激活的新任务显示"已结束"；过期任务点"开始收集"后仍显示已过期；删除失败被空 catch 吞掉 |
| H31 | ReviewHistory 撤回弹窗不说明后果 | `ReviewHistory.vue:123-129,88-91` | 不说明撤回"上传/修改/批改"后会发生什么、批量是整批还是单条；不可撤回的操作也一律显示按钮 |
| H32 | 统计图表空数据空白、失败静默全 0 | `DashboardStats.vue:123-189` | 无数据时是空白画布；接口失败静默显示全 0，用户分不清"没数据"还是"加载失败" |
| H33 | 列表接口返回全量正文 | `essays.py:2759-2760` | 列表每页 100 条都带完整 content_text/corrected_text，数据量大时列表加载变慢；列表只需字数，应只返回摘要 |
| H34 | main.js 全量引入 Vant | `main.js:4-10` + `vite.config.js` | 已配置 VantResolver 按需解析，却仍 `app.use(Vant)` + 全量 CSS，入口 chunk 偏大（dist 155KB），应改按需 |
| H35 | 多页面空状态误导/缺失 | `EssayList.vue:203`、`ReviewPending.vue:125-127`、`AdminTasks.vue:139-141`、`ReviewHistory.vue:109`、`DashboardStats.vue` | 筛选无结果显示"没有待批作文/请调整筛选条件"，无法区分"系统无数据"与"筛选无结果" |
| H36 | AdminSettings testServer 未带鉴权头 | `AdminSettings.vue:317` | 与 testDb（带 Bearer）不一致，若接口需登录态则检测永远失败 |

### 🟡 低优先级（体验提升）

| # | 问题 | 位置 | 说明 |
|---|------|------|------|
| L1 | AccountSettings.vue 死代码 | `views/AccountSettings.vue` | 与 PersonalInfo.vue 几乎重复，未在 router 引用；应删除或合并 |
| L2 | AdminUsers 手机端无删除/改他人密码入口 | `AdminUsers.vue:41-55` | 桌面端有"删除/改密"，手机端只有编辑图标，能力不对齐 |
| L3 | 角色与标题命名不一致 | `App.vue:88`、`EssayList.vue:48`、`index.html:2,7` | reviewer 在部分页面叫"修改者"、部分叫"批改者"；index.html 标题"作文修改系统"与系统名"作文收集管理系统"不一致；lang="en" |
| L4 | CSV 导出未按 RFC4180 转义 | `EssayList.vue:895` | 标题/备注含引号或换行时 Excel 打开列错位 |
| L5 | 列表页细节 | `EssayList.vue` 多处 | 表头不吸顶（108-126）；跳页输入框不随当前页更新（355）；字数 0 与无数据不分（141-142）；桌面补交标记无 title（137）；「文件」列撞状态色（136）；状态按字典序排序（396）；加载中旧数据并存（100）；移动端无每页数量选项（86-87）；任务/批改者下拉失败静默（835-847） |
| L6 | 详情页细节 | `ReviewDetail.vue` 多处 | 加载失败整页空白无重试（984-987）；可保存空文本清空整篇（838-847）；无 Ctrl+S/Esc（831,888）；「⛶ 双全屏」文案歧义（167）；手机端 corrected 后无法编辑批改者备注（389-409）；mobileTab 死代码（550）；进入/退出编辑整棵重建 DOM 长文卡顿（197,263） |
| L7 | 上传页细节 | `EssayUpload.vue` 多处 | 409 弹窗按钮主次颠倒（514-527）；拖拽不支持格式静默丢弃（349-373）；压缩过程无进度提示（322-335） |
| L8 | 批量页细节 | `EssayBatchUpload.vue` 多处 | 未选任务无提醒（913-931，单页上传有）；并发进度显示陈旧姓名（141,152）；8MB 图片跳过无预告（102,531）；iOS Safari 不支持 webkitdirectory 报错费解（80-81） |
| L9 | ReviewPending 细节 | `ReviewPending.vue:49-53,498-510` | 字数 min>max 无交叉校验；流水线未勾选时处理"当前页"但弹窗不说明作用域 |
| L10 | AdminTasks 细节 | `AdminTasks.vue:21,145,455-486` | 截止早于开始不校验；"第几次"筛选写死 1-10；编辑弹窗点遮罩丢修改；"结束收集"无确认 |
| L11 | AdminSettings 细节 | `AdminSettings.vue:6-14,24,104-106,199-201` | 手机端无分区导航整页长滚；端口可输字母；服务商下拉只有一项是死控件；字数倒挂无校验 |
| L12 | ReviewHistory 细节 | `ReviewHistory.vue:158,109` | 操作筛选项缺"AI 错别字修正/AI 改写/流水线"等新操作类型；手机端空列表显示"没有更多了" |
| L13 | DashboardStats 无刷新/自动刷新 | `DashboardStats.vue:184-189` | 只在进入页面取一次数据，批改进行中需手动重进 |
| L14 | 登录页无「忘记密码/联系管理员」提示 | `Login.vue` | 忘记密码只能找管理员，页面无任何指引 |

---

## 三、后端性能与稳定性待办（此前已确认，尚未实施）

### 性能（对应大数据量卡顿根因）

- [ ] 消除 N+1：列表文件系统 stat、操作日志 user/essay 逐行查询（见 G16）
- [ ] 作文图片由数据库（`EssayImage.image_data`）迁移到文件系统
- [ ] 常用筛选加索引（含 `essays.course_id` 补 `idx_essays_course_id`）
- [ ] 批量操作（含删除/导出）分批处理，避免全量加载内存风险
- [ ] 后台任务引入队列/限流/进度持久化，替代裸线程（见 G17）

### 稳定性与备份

- [ ] 引入 Alembic 版本化迁移（替代手写 ALTER）
- [ ] 数据库密码配置落盘与连接串转义（见 G54）
- [ ] 统一错误处理与日志落盘（对应"错误提示不足"痛点）
- [ ] 定时自动备份数据库与文件（保留 N 份策略）
- [ ] 下载/导出临时文件请求后自动清理（见 G19）

### 报表（用户已确认需要）

- [ ] 批改工作量/进度报表：按课程/任务/时间维度的完成率、批改者工作量
- [ ] 一键导出 Excel

### 功能规划（行业对标）

- [ ] R6 评分与评语体系（分数/等级/评语模板，支撑报表与成长统计）
- [ ] R8 结构化日志 + 错误追踪（含前端错误提示统一优化）
- [ ] R7 逐句对照视图（**优先级靠后**，复杂度高，用户已确认延后）
- [ ] R5 学生名单管理（用户暂缓，先记录）
- [ ] ZIP 压缩包上传（手机端 webkitdirectory 不可用，后续按需补充）

> 已确认暂不需要：在线批注/圈画、错误归类统计、多版本管理、缺交名单/催交、任务分配/转派、消息通知、批改快捷键、Docker 部署、自动化测试。

---

## 四、待确认问题（Open Questions）

- [ ] 数据可见范围：目前收集者可看全部作文，是否需要收窄为「只看自己收集/自己班级」？（关联 G7）
- [ ] 图片存储：是否同意将作文图片从数据库迁出到文件系统？
- [ ] 报表粒度：批改工作量报表需要哪些具体指标（完成率/篇数/字数/耗时/漏改率）？
- [ ] 学生成长/缺交统计报表是否纳入本轮？
- [ ] 自动备份周期与保留份数如何设定？
- [ ] 手机端兼容性具体在哪些机型/浏览器出现问题？（需复现案例）
- [ ] 评分体系：分数制还是等级制？是否要评语模板库？
- [ ] 逐句对照：对比基于纯文本 diff 即可，还是需要解析 docx 段落结构？（影响复杂度与排期）
- [x] 批量 AI 操作是否允许收集者使用？→ **已定：仅批改者/管理员**（`ReviewPending` 路由 roles 不含 collector，收集者本就不达该页；前端按钮按角色隐藏，后端仅 admin/reviewer 可启动，见 G6/G24）

---

## 五、实施规范

- 未获用户确认前，不实施任何改动
- 每轮改动范围需与用户确认后再动手
- 改动用例：性能优化、安全修复、报表开发、前端体验修复均需回归验证
- 代码不新增注释（遵循现有风格），遵循仓库既有规范
