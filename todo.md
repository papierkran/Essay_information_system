# 作文收集管理系统 · 改进清单

> 状态说明：本文件为需求调研与改进记录。
> **本次更新（2026-08-09）：以前端/用户视角重新审视全项目，新增「前端体验优化」清单；已完成的优化项（安全加固 S1-S8、课程实体化 R10、任务筛选搜索 R11、rework 状态、多线程批量任务、合并导出等）已从本文件删除。**

---

## 一、前端体验优化（本次审查新增，待实施）

> 从"用户是否看得懂、用得准、用得顺"三个角度审查 `frontend/src` 全部 14 个页面与基础设施后整理。

### 🔴 高优先级（Bug / 明显误导）

| # | 问题 | 位置 | 说明 |
|---|------|------|------|
| F1 | ~~409 冲突跳转失效路由~~ ✅ 已修复 2026-08-09 | `EssayUpload.vue:319` | 原跳转不存在的路由 `#/essays`，已改为 `#/essay/list` |
| F2 | ~~作文列表手机端无适配~~ ✅ 已修复 2026-08-09 | `EssayList.vue` | 新增手机端卡片列表（勾选+状态标签+标题/任务/收集者元信息+时间+详情入口），复用现有筛选与分页；桌面表格保留 |
| F3 | ~~游客可见但不可用的入口~~ ✅ 已修复 2026-08-09 | `App.vue`、`Dashboard.vue` | 侧边栏「上传作文/批量上传」对游客置灰不可点并提示；工作台「开始上传」快捷入口对游客置灰 |
| F4 | ~~未改列表排序误导~~ ✅ 已修复 2026-08-09 | `ReviewPending.vue` + `essays.py:758-768` | 原仅对当前页前端排序，已改为后端排序（后端 `allowed_sort` 增加 teaching_mode/word_count/corrected_word_count，前端排序时带参重查） |
| F5 | ~~CSV 导出只含当前页~~ ✅ 已修复 2026-08-09 | `EssayList.vue:67` | 按钮改名为「导出CSV(当前页)」并加 title 提示仅导出当前页 N 条 |

### 🟠 中优先级（准确性与一致性）

| # | 问题 | 位置 | 说明 |
|---|------|------|------|
| F6 | ~~详情页无状态徽标~~ | `ReviewDetail.vue` | 顶部无 pending/confirming/rework/corrected 状态标识，用户/批改者看不清作文处于流程哪一步；建议顶部醒目状态标签 + 流程提示 |
| F7 | ~~字数口径不一致~~ ✅ 已修复 2026-08-09 | 后端 `essays.py` + `ReviewDetail.vue` + `utils/format.js` | 统一为「不含空格/换行等空白字符，标点计入」（`_count_non_ws` / `_char_count_sql` / `countWords`），列表筛选与字数排序也改用同一 SQL 表达式 |
| F8 | ~~AI 修正反馈信息不友好~~ ✅ 已修复 2026-08-09 | `ReviewDetail.vue` | `doAiCorrect` 成功提示改为「AI 修正完成（当前 N 字）」，不再展示 title/author 元信息 |
| F9 | ~~上传成功弹窗信息不完整~~ ✅ 已修复 2026-08-09 | `EssayUpload.vue` | 弹窗增加「去列表查看」；新增「连续上传时保持当前任务选择」勾选，高频连传不再重选任务 |
| F10 | ~~浏览器标签标题不更新~~ ✅ 已修复 2026-08-09 | `router/index.js` | `router.afterEach` 按路由统一设置「页面名 · 作文收集管理系统」（详情页仍由组件标题覆盖） |

### 🟡 低优先级（体验提升）

| # | 问题 | 位置 | 说明 |
|---|------|------|------|
| F11 | ~~系统设置页无导航~~ ✅ 已修复 2026-08-09 | `AdminSettings.vue` | 新增吸顶分区锚点导航（数据库/服务地址/存储目录/OCR/错别字修正/AI改作文/备份），点击平滑滚动到对应卡片 |
| F12 | ~~作文列表筛选过密~~ ✅ 已修复 2026-08-09 | `EssayList.vue` | 常用筛选常驻，其余折叠到「更多筛选」（提交方式/收集者/任务/批改者/补交/时间/字数/备注）；若已启用高级筛选则自动展开 |
| F13 | ~~工作台请求低效~~ ✅ 已修复 2026-08-09 | `Dashboard.vue` + `essays.py` | 「最近上传」改为 `page_size=10` 定向拉取；新增 `POST /essays/tasks/stats` 批量统计接口，活跃任务统计由 N 次请求合并为 1 次 |
| F14 | ~~任务进度条仅管理员可见~~ ✅ 已修复 2026-08-09 | `App.vue` | `TaskStatusBar` 移除 isAdmin 条件，任何角色启动批量任务后均可看到全局进度 |
| F15 | ~~编辑无脏数据保护~~ ✅ 已修复 2026-08-09 | `ReviewDetail.vue` | 新增 `isDirty` 检测 + `beforeunload` 提示 + `onBeforeRouteLeave` 离开确认（覆盖基本信息、正文编辑、重新上传内容） |
| F16 | ~~已保存账号无法管理~~ ✅ 已修复 2026-08-09 | `Login.vue` | 已保存账号可一键删除（含当前激活账号）；清理死代码 `keyName` |
| F17 | ~~未改列表手机端功能缺失~~ ✅ 已修复 2026-08-09 | `ReviewPending.vue` | 手机端与作文列表同步：卡片样式统一（勾选+状态标签+标题/任务/收集者+类型+详情）、新增折叠式筛选面板（姓名/标题/状态/年级/方式/收集者/任务/次数/日期）、批量操作工具栏与分页手机端可用 |
| F18 | ~~错误提示不统一~~ ✅ 已修复 2026-08-09 | `api/index.js` | 新增 `getApiErrorMessage`（状态码→中文 + 网络/超时兜底）；响应拦截器对无 detail 的错误统一弹中文提示，保留原有 401 处理 |
| F19 | ~~表格禁止复制文本~~ ✅ 已修复 2026-08-09 | `EssayList.vue` | 移除 `.table-wrap` 的 `user-select: none`，学生姓名等文本可正常选中复制 |
| F20 | ~~批量删除逐个请求~~ ✅ 已修复 2026-08-09 | `essays.py` + `EssayList.vue` | 新增 `POST /essays/batch-delete`（delete_file/permanent 语义与单条一致），前端批量删除一次请求完成，失败明细计数返回 |
| F21 | 无前端工程化 | `frontend/package.json` | 无 eslint/测试脚本，回归靠人工 → 加 lint + 关键流程冒烟测试（**暂不处理**） |

---

## 二、后端待办（此前已确认，尚未实施）

> 均来自早前代码审查，与前端体验无直接冲突，保持原优先级。

### 性能（P1-P8，对应大数据量卡顿根因）

- [ ] 消除 N+1：列表文件系统 stat、操作日志 user/essay 逐行查询
- [ ] 作文图片由数据库（`EssayImage.image_data`）迁移到文件系统
- [ ] 常用筛选加索引（含 `essays.course_id` 补 `idx_essays_course_id`）
- [ ] 批量操作（含删除/导出）分批处理，避免全量加载内存风险
- [ ] 后台任务引入队列/限流/进度持久化，替代裸线程

### 稳定性与备份

- [ ] 引入 Alembic 版本化迁移（替代手写 ALTER）
- [ ] 数据库密码配置落盘与连接串转义
- [ ] 统一错误处理与日志落盘（对应"错误提示不足"痛点）
- [ ] 定时自动备份数据库与文件（保留 N 份策略）

### 报表（用户已确认需要）

- [ ] 批改工作量/进度报表：按课程/任务/时间维度的完成率、批改者工作量
- [ ] 一键导出 Excel

### 功能规划（行业对标）

- [ ] R6 评分与评语体系（分数/等级/评语模板，支撑报表与成长统计）
- [ ] R8 结构化日志 + 错误追踪（含前端错误提示统一优化）
- [ ] R7 逐句对照视图（**优先级靠后**，复杂度高，用户已确认延后）
- [ ] R5 学生名单管理（用户暂缓，先记录）

> 已确认暂不需要：在线批注/圈画、错误归类统计、多版本管理、缺交名单/催交、任务分配/转派、消息通知、批改快捷键、Docker 部署、自动化测试。

---

## 三、待确认问题（Open Questions）

- [ ] 数据可见范围：目前收集者可看全部作文，是否需要收窄为「只看自己收集/自己班级」？
- [ ] 图片存储：是否同意将作文图片从数据库迁出到文件系统？
- [ ] 报表粒度：批改工作量报表需要哪些具体指标（完成率/篇数/字数/耗时/漏改率）？
- [ ] 学生成长/缺交统计报表是否纳入本轮？
- [ ] 自动备份周期与保留份数如何设定？
- [ ] 手机端兼容性具体在哪些机型/浏览器出现问题？（需复现案例；F2 桌面表格直接导致手机体验差）
- [ ] 评分体系：分数制还是等级制？是否要评语模板库？
- [ ] 逐句对照：对比基于纯文本 diff 即可，还是需要解析 docx 段落结构？（影响复杂度与排期）

---

## 四、实施规范

- 未获用户确认前，不实施任何改动
- 每轮改动范围需与用户确认后再动手
- 改动用例：性能优化、安全修复、报表开发、前端体验修复均需回归验证
- 代码不新增注释（遵循现有风格），遵循仓库既有规范
